import type { App } from '../../kernel/app.js';
/** /workflow — live registry view of workflow runs (phases, agents). */
export declare const workflowCommand: (app: App) => void;
export declare function installWorkflowCommand(app: App): void;
