import type { App } from '../../kernel/app.js';
/** /mcp — MCP tools grouped by server (prefix mcp__<server>__<tool>). */
export declare const mcpCommand: (app: App) => void;
export declare function installMcpCommand(app: App): void;
