import type { App } from '../../kernel/app.js';
/** /theme [name] — built-in presets over the colorscheme. */
export declare const themeCommand: (app: App, a: string | undefined) => void;
export declare function installThemeCommand(app: App): void;
