import type { App } from '../../kernel/app.js';
/** /config — current runtime summary. */
export declare const configCommand: (app: App) => void;
export declare function installConfigCommand(app: App): void;
