import type { App } from '../../kernel/app.js';
/** /status — active session snapshot. */
export declare const statusCommand: (app: App) => void;
export declare function installStatusCommand(app: App): void;
