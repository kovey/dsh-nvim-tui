import type { App } from '../../kernel/app.js';
export declare const densityCommand: (app: App) => void;
/** /glance [segment…] — toggle statusline segments. */
export declare function installDensityCommand(app: App): void;
