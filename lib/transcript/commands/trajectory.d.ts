import type { App } from '../../kernel/app.js';
export declare const trajectoryCommand: (app: App) => void;
/** /export — write the rendered transcript to a markdown file. */
export declare function installTrajectoryCommand(app: App): void;
