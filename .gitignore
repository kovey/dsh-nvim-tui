# dependencies / package managers
node_modules/
.pnpm-store/
.npm-cache/

# dsh test profile: local harness state, not project source
.dsh-test/

# project-local dsh memory (per-machine notes, not project source)
.dsh/

# local spec notes (kept out of version control on purpose)
REQUIREMENTS.md

# logs
*.log

# OS junk
.DS_Store
