# neovim-tui v0.3.3 全面代码审查报告

> 审查日期：2025-09-08 · 基线：main @ 53fa5d8 · 全程只读，本报告落盘后按 §5 修复清单逐一修复
>
> 范围：src/ 全部 93 个 TS 文件（12,593 行）+ nvim/lua/dsh_tui/ 18 个 Lua 文件（5,223 行）+ scripts/examples/README/UPGRADE/REQUIREMENTS/CHANGELOG/docs
>
> 方法：7 个并行深度审查（kernel/boot、feed/statusline/market、commands、sessions/transcript/subagents、ext-api/deps/scripts、Lua 前端、文档一致性）+ 机械检查（tsc strict、lib 产物同步、死代码扫描、npm audit）。全部 🔴 结论均经父代理亲自复核代码确认。

## 0. 环境级机械检查结果（全绿）

- `tsc --noEmit`（strict + noUnusedLocals/noUnusedParameters）零错误；`tsconfig.scripts.json` 零错误
- `check-arch.mjs` ✓、`app-ops-check.mjs` ✓（76 域 op 全注入）
- `lib/` 与全新编译产物逐字节一致；`npm audit` 0 漏洞；无 debugger/console 调试残留；无 TODO/FIXME
- 死代码扫描：Lua 无死 local 函数/孤儿 M.* 导出；TS 侧 ~50 个多余 export（仅同文件内使用）、~30 个类型导出全库无人引用、10 个仅测试引用；全库 40+ 处空 `catch {}`
- RPC 契约：Lua→Node 23 个 dsh-* notify 与注册 1:1；Node→Lua 26 个 luaCall 方法签名一致；dsh-ext 请求/响应协议匹配；EXT_API_VERSION 两侧 0.1.0

## 1. 🔴 严重问题（7 项，已逐一验证）

| # | 位置 | 问题 | 后果 | 修复方案 |
|---|------|------|------|----------|
| R1 | `src/commands/core.ts:578-582` | `settleApproval` 内部已 `advanceApproval()` 把排队审批提升为前台（approvalSettle 指向新条目），随后无条件 `setApproval(null, null)` 把刚提升的 settle 抹成 null | 父代理+子代理并发审批时，第二个审批浮窗作答后 `fn` 为 null，宿主侧 Promise 永不 resolve → 工具调用卡死 | 删除 `app.slices.agent.setApproval(null, null)` 行（settleApproval 已自清） |
| R2 | `src/commands/core.ts:584-593` | dsh-questions-answered/cancelled 直接 `questionsResolve?.resolve(...)` + `setQuestions(null)`，绕过 `settleQuestions`/`rejectQuestions`（含 `advanceQuestions()`，全仓库零调用者=死代码） | 并发第二个 user-questions 永不显示、永不 settle，同样挂起 | answered 改走 `app.slices.agent.settleQuestions(answers)`；cancelled 改走 `app.slices.agent.rejectQuestions()` |
| R3 | `src/ext-api/index.ts:73-84, 215-217` | `fireExtReady` 把 `WE.extReadyResolve = null` 而**从不调用**它；`readyWaiters` 无人 push | `await tui.ready` 永久挂起；examples/dsh-plugin 的 `tui.ready.then(...)` 内全部初始化（状态栏段/命令/事件订阅）静默瘫痪 | fireExtReady 里 `WE.extReadyResolve?.()`（或恢复 readyWaiters push 模式） |
| R4 | `src/ext-api/index.ts:440-452` | `luaExt.on` 的 token 守卫形同虚设：disposer 无条件 `extNodeHandlers.delete(extId)`，无视 token | 同 extId 二次注册后，旧 disposer 误删新 handler，扩展总线静默失联 | disposer 改为 `if (get(extId)?.token === token) delete` |
| R5 | `src/transcript/commands/rewind.ts:35-37` | `/rewind N` 用 `boundaries[boundaries.length - n]` 从**末尾**倒数：`/rewind 1`=空操作、`/rewind 2`=删光除最后 2 条外全部历史；数字路径无确认弹窗，truncate 就地持久化不触发事件 | 一行误输入造成不可恢复的历史删除，方向与用户直觉相反 | 数字参数与 picker 语义对齐（第 N 条=从开头数，truncate 到其后）；数字路径加确认 picker |
| R6 | `nvim/lua/dsh_tui/session.lua:105-122` | **文件结构性损坏**：`SE.toggle_reasoning` 的 `end` 丢失，`function S.close_chat(id)` 被嵌套进 toggle 函数体内（挂在共享状态表 S 上），尾部 `return S` + `.reasoningOpen` + 游离 `end`。`init.lua:145` 的 `M.close_chat = SE.close_chat` 导出 **nil** | CHANGELOG v0.3.3「live 会话 LRU 软上限 16」从未生效（Node 侧调用者也从未存在）；未来启用时 luaCall 抛 nil call | 把 close_chat 移出 toggle_reasoning，改为 `function SE.close_chat(id)`，删除 `return S.reasoningOpen` 残片，补 toggle 的 end |
| R7 | `nvim/lua/dsh_tui/init.lua:373`、`autocmds.lua:365` | 使用 `vim.uv`（nvim 0.10 才引入）无兼容降级 | README 承诺「0.9 可运行降级」，实际 0.9 上 M.start() 直接崩溃 | 改 `(vim.uv or vim.loop)` |

## 2. 🟠 主要问题（按模块，共 33 项）

### 2.1 kernel/boot（5）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| K1 | boot.ts:71-176 | teardown/quit 撞上 `await spawnNvim` 窗口后 boot 继续在已 disposed 的 app 上运行：把死 child 写回 runtime、静默 console、连接 nvim、重新武装 `idleRefreshTimer`（永不再清理）、在死 app 上建会话 | boot 每个 await 后检查 `runtime.disposed` 直接 return |
| K2 | boot.ts:87-92,177-184 | nvim 启动失败：onExit 先 `quit(0)`，10s 超时后 `quit(1)` 因 quitting=true 变 no-op → 退出码 0（CI 误判）；fatal 的 console.error 已被 :98-101 劫持成 silent，终端不可见 | onExit 在 boot 完成前记录 deferred exit；fatal 用 `process.stderr.write` |
| K3 | bridge.ts:158-167 | `await nvim.channelId` 握手无超时；headless watchdog 布防在其后 | channelId 加 Promise.race 超时，或 spawn 后立即 arm watchdog |
| K4 | lifecycle.ts:138-145 | quit 兜底 `setTimeout(() => process.exit(code), 2000)` 可截断进行中的会话 flush（zstd 尾帧写一半=损坏） | race 失败后对 flush 再给受保护的有界等待 |
| K5 | app.ts:496-505 | openPicker 陈旧失败回调无条件 `settlePicker(null)`，把新 picker 当取消结算 | catch 里校验 `pickerSettle === resolve` 身份 |

### 2.2 feed/statusline/market（5）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| F1 | feed.ts:1203-1214 | 表格检测在 diff 区域判断之前执行，diff 上下文里的 markdown 表格行被误渲染成边框表格（注释「Diff rows are handled below and never reach this check」与代码顺序矛盾） | 把 diffRegion 判断提到表格检测之前（或 isTableRow 排除两空格前缀行） |
| F2 | market/commands/market.ts:108+145-149 | 自卸载保护恒假：togglable 已排除 `name !== 'dsh-nvim-tui'`，isSelf 在排除后的集合里查找 → 恒 false | 修复过滤/判断顺序 |
| F3 | feed.ts:400-409 | `pushExtCard.update` 每次从创建时原始 opts 合并，连续部分更新静默丢修改 | 捕获「上次 merged」而非初始 opts |
| F4 | statusline/index.ts:113-158 | jobs 板去重自相矛盾：commit 后 `cache.delete` 被下一轮心跳 merge 重填抵消 → 旧板随新批次重复提交进聊天流 | 用 committedJobsKey 排除已提交条目（如改为差分提交或持久记录已提交 id） |
| F5 | feed.ts:1189-1196 | 流式表格「开表不画底边框」被 pinned 面板/进度行破坏（只排除了 trailingStatic） | 把 panelLines/progressLines 也计入尾部透明区 |

### 2.3 commands（6）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| C1 | core.ts:437-443,490-502 | tui_command 白名单含 `/settings` `/memory` `/tasks`：可带参执行 `/settings set`（无确认改写 settings.yaml）、`/memory delete`、`/tasks kill`，与「destructive actions stay out of reach」注释矛盾 | 白名单移除 /settings、/memory（/tasks 评估）；或按 args 二次过滤 |
| C2 | exit.ts:10 / quit.ts:10 / restart.ts:9-18 + nlcmd.ts:59 | 退出类命令零确认；`['q']` 精确路由 + 即时执行：聊天框误输单字符 q 即退出整个进程 | 三命令加确认 picker；nlcmd 删 `['q']` 路由 |
| C3 | nlcmd.ts:135 + yolo.ts:11 | 「开启yolo/关闭yolo」路由到无参 toggle，方向 50% 相反 | exact 条目补固定参数（['开启yolo','on']…）；yolo.ts 无效参数报用法 |
| C4 | model.ts:11-25 | /model 无参「浮窗选择」只有一个条目（声明未实现）；带参不校验模型 id，错误 id 直接持久化为默认模型 | 无参复用 /models 目录数据源；带参校验后再 saveSelection |
| C5 | image.ts:32 | /image 相对路径按 process.cwd() 解析，违反 kernel 契约（其余命令均 join(activeSessionCwd)） | 改 join(activeSessionCwd(app), path) |
| C6 | memory.ts:17-32 | /memory delete 零确认（路径穿越防护正确） | 加确认 picker |

### 2.4 sessions/transcript/subagents（6）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| S1 | transcript/index.ts:248-251 + rewind.ts:52-57 | rewind 重建聊天后，已渲染的 ✎ diff 块全部消失：`renderedDiffCalls` 去重缓存不随 feed.clear 失效 | feed.clear 时同步清除该 feed 的 renderedDiffCalls 条目 |
| S2 | services.ts:111-113 | ensureLiveSession 无 in-flight 去重，并发 resume 同会话产生双 live 记录/双 FeedRenderer 写同一批 buffer | 加 resuming in-flight Set/Map |
| S3 | sessions/index.ts:212-240 | refreshHistory 静默吞错（catch{}）：存储故障时 UI 呈现「会话全消失」且 boot 静默建新会话 | 失败给 notice + errorLog |
| S4 | workspace.ts:22-24,57-59 | `ws.create?.()` 未装配时 await undefined 后照样提示「已添加」——UI 假成功 | 显式检查服务存在，缺失提示「服务未装配」 |
| S5 | sessions.ts:74-79 + services.ts:111-136 | 重命名后台 resume 的会话永不释放，反复重命名无界累积 live 会话 | rename 完成后若非 activeId 则 dispose |
| S6 | services.ts:17-24 | 后台 resume 无条件 setChatWin/setReasoning（全局状态），活动会话思考面板指向后台会话窗口 | attachSession 分 active/background 两路径，后台路径不动全局视图状态 |

### 2.5 ext-api/deps/scripts（5）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| E1 | ext-api/index.ts:188-198,396-403 | `nvim.request` 的 `method.startsWith` / `registerCommands` 的 `c.name.replace` 对垃圾输入同步抛 TypeError 崩调用方 | 入参类型守卫，与 call 一致干净 reject |
| E2 | ext-types.ts:13-16 vs 实现 vs 示例 | 文档称「无白名单任意 vim.fn」，实现强制 nvim_* 前缀 + SAFE_VIM_FN 白名单，且白名单含可执行任意命令的 systemlist（示例用它跑 git log）——「只读」声明失实 | 三方对齐：明确契约（白名单收窄或文档改口），systemlist 去留显式决策 |
| E3 | scripts/smoke.ts:2842-2847 | graceful-exit 断言被 kill 路径旁路（实测 nvim 死于 SIGTERM 仍 SMOKE PASS） | kill 路径记为失败或显式 warn+断言原因 |
| E4 | src/deps/services.ts:195 | `host('session-stats', 'session-stats', ...)` 唯一不带 camelCase 服务键的行，就绪检测几乎永远为假 | 改 `sessionStats` |
| E5 | scripts/e2e.ts:57-83 | 验收口径弱：不校验 dsh 退出码、非空文本≠模型响应 | dump 后仍校验进程退出码；断言强化 |

### 2.6 Lua 前端（6）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| L1 | autocmds.lua:99-105 | input buffer 被 wipe 后自愈重建缺 `nvim_win_set_buf(S.input_win, S.input_buf)` → 新 buffer 无人显示，输入框永久失效 | 重建后挂载 + 重装窗口家具 |
| L2 | api.lua:783-788,854-859 | top/bottom 区域 q/Esc 键位不传 side，region_release 默认 'right' 查不到 → 区域永远关不掉 | keymap 传 `region_release(id, 'top'/'bottom')` |
| L3 | at_menu.lua:146-176 | @补全无代数校验：陈旧响应复活菜单并吞用户已键入草稿 | AM.set/accept 前核对 query/start 与当前输入一致 |
| L4 | full_input.lua:72-75 | footer 键无 `has('nvim-0.10')` 守卫（全仓库唯一漏处），0.9 上 <C-e> 必崩 | 加守卫 |
| L5 | api.lua:807 | region_claim 的 nvim_open_win 无 pcall，违反 `{err}` 返回契约，Node 侧变 unhandled rejection | pcall + `{err}` |
| L6 | session.lua:81-85 | toggle_reasoning 兜底创建「幽灵」buffer：不登记 reasoningBufs、可编辑、每次泄漏一个 | 登记 + 锁定 + 选项齐全（与 R6 同批修复） |

### 2.7 文档实质性漂移（2）

| # | 位置 | 问题 | 修复方案 |
|---|------|------|----------|
| D1 | README L55、L264-284 + REQUIREMENTS R-IMG-3/4 | 「识图桥 vision-bridge」整节描述已移除的旧行为（现为官方识图模型自动切换） | 重写该节 |
| D2 | CHANGELOG v0.3.3 L68-69 | 「live 会话 LRU 软上限 16」查无实现（对应 R6） | 修复 R6 后保留声明；或降级为「已移除」 |

## 3. 🟡 次要问题清单（72 项，按模块）

<details>
<summary>kernel/boot（11）</summary>

- boot.ts:98-101 console 进程级劫持永不恢复（hmr 后告警全静音）→ 与 K2 同批治理
- session-events.ts:193-202 图片毒化检测只处理 `{message}` 形状，直连 ChatMessage 形状漏检
- session-events.ts:129 pendingFileSnaps.clear() 全局清空，跨会话并发回合快照互相踩踏
- session-events.ts:154-156 / app.ts:435 模板插值后调 t() 永远查不到字典键（en 恒回退中文）；app.ts:171 未包 t()
- app.ts:531-532 process.on('uncaughtException') 抑制默认崩溃，非 fail-loud 宿主带未定义状态续跑
- app.ts:399 `{} as unknown as AppSlices` 双重断言，slice 形状全靠外部脚本守护
- app.ts:135 SessionRec 尾随索引签名使拼错属性静默通过 strict
- bridge.ts:68-74 mkdtemp 每次 spawn 泄漏一个临时目录（已实证 /tmp 残留）；app.ts:488 dumpPath 追加无 try/catch
- bridge.ts:123-128 spawn 失败 'error'+'exit' 可能双触发 onExit（当前因 quit 幂等无害）
- lifecycle.ts:118/137 quit 路径 closeNvimWindow 调用两次
- lifecycle.ts:82-84 teardown 直接调 drainApprovals/drainQuestions/settlePicker 无 try/catch
- headless.ts:43-46 watchdog 未 unref 且布防晚（并入 K3）
</details>

<details>
<summary>feed/statusline/market（14）</summary>

- feed.ts:755 `(this.eventTime ?? Date.now())` 死回退（?? 应 ||）→ 无时间戳时显示 0ms
- feed.ts:681-704 user/message 不 commitTail，中断回合输出挂在新回合标记后
- diff.ts:127-131 wholeReplace 统计 +N −M 虚高（上下文行双计）
- feed.ts:616-618,606-613 UTF-16 截断撕裂 emoji；argsPreview 重复 stringify
- market/progress.ts:427-428,604-620 坏 tarball 安装无修复候选（npm 回退被制度性排除）
- market/progress.ts:44-56 registryBase 声称支持镜像但 codeloadUrl 只认 raw.githubusercontent.com
- feed.ts:736-737 tool/call 无 callId 共用 '' 键；calls Map 永不清理（慢泄漏）
- feed.ts:205 + statusline/commands/whale.ts:10 「有内容时底部水印」未实现（声明未实现）
- feed.ts:1155-1290 每 flush 全量重解析视图 O(n)/事件（长会话退化）
- market/commands/market.ts:61 readInstalledPlugins 每依赖同步 readFileSync 阻塞主线程
- market/commands/market.ts:80-87 更新检查静默截断前 10 个依赖
- market/progress.ts:352 latestCache 永不清空
- feed.ts:893-901 workflow 子代理行带两空格前缀，ROLE_BY_PREFIX 不匹配丢失着色
- feed.ts:1391-1426 in-place 行更新跳过 syncCardMarks（ext card 恰为最后一行时陈旧）
- feed.ts:590-600 structuredHits 对非对象调用 truncate → '[object Object]'
</details>

<details>
<summary>commands（8）</summary>

- settings.ts:83 浮窗帮助文案 2 参 vs 实现 3 参
- README /yolo 文档「全放行」vs 实现 fail-closed 自动拒绝（语义相反）
- nlcmd INTENTS 与命令目录不同步：/dir /lines /history（及 /deps /branch）无自然语言入口；「历史」被路由到 /sessions
- yolo.ts:11 无效参数静默 toggle
- image.ts:32 / attach.ts:28 readFileSync 无大小上限阻塞主线程（lines 有 256KB 上限可对照）
- permission.ts:40 危险预设确认靠名字正则启发式（'unsafe' 绕过）
- usage 字段语义混用，/help 显示冗余怪异
- tasks.ts:26-27 kill 非 requested 一律显示「已结束」（未知 id 误导）
</details>

<details>
<summary>sessions/transcript/subagents（11）</summary>

- rewind.ts:49-59 截断后 SessionRec 派生状态（goal/plan/todos 等）不同步；未检查回合 running
- sessions/index.ts:243-256 refreshList 不过滤归档会话（三处视图两处过滤）
- services.ts:161-162 switchTo 先改 activeId 再 await setActive，失败无回滚
- subagents/index.ts:233-234 兜底把子代理路由到当前活动会话（脏路由）
- subagents/index.ts:295-304 subagent/end 丢失时 runningSubagents 条目永不清除（状态行永久 running）
- transcript/index.ts:130-135 孤儿修复 have 集合扫描 events 而非 surface
- queue.ts:39-41 「清空全部排队」无确认
- subagent-clean.ts:5-7 文件头注释声明的清理机制与实现不符（truncateStored 不存在）
- sessions.ts:150-151 「新建会话于此工作区」不 attachSession，下次出现在未分组
- sessions/index.ts:123-125 cleanSubagentChain writeFileSync+renameSync 无 fsync 非原子；writeCleanedIds 吞错
- rename.ts:22-27 重命名后不刷新缓存，依赖宿主 emit（契约未声明）
</details>

<details>
<summary>ext-api/deps/scripts（8）</summary>

- smoke.ts:785-786 断言恒真（打开面板后自比）
- smoke.ts:1040 硬编码 47 条 fallback 命令（维护地雷）
- smoke.ts:2291 性能断言 <2500ms flaky
- package.json smoke 脚本不先 build（干净 checkout 测过期 lib 产物）
- ext-api/index.ts:193-197,424-427 Promise.race 超时定时器不清理（大 timeoutMs 拖住事件循环）
- examples/dsh-plugin/index.ts:40-47 卡片声明 action 无 onAction（按 1 静默无效）
- examples/nvim/git-panel.lua:36,75 pcall(vim.fn.system) 同步阻塞（教坏纪律）；:44-46 死参数 + r 键位重开丢失
- deps/services.ts:302-315 装配无备份/回滚（appendFileSync 直写）
</details>

<details>
<summary>Lua 前端（8）</summary>

- popups.lua:56,437 show_skill/show_lines_float 不关旧窗（旧 float 滞留）
- autocmds.lua:92-352 全局 autocmd 无 augroup（state 置 nil 后二次 start 双注册）
- at_menu.lua:137-138 光标停在 token 中间时 accept 拼接重复片段
- input.lua:111-118,38 I.append/I.set_text 无 valid 守卫
- popups.lua:307,573,700 无条件设 title（0.8 会失败，低于最低版本仅提示）
- popups.lua:525-530 close_progress 不回焦输入（体验不一致）
- cmd_menu.lua:50 entries 用 pairs 合并，扩展命令顺序随机
- api.lua:798 bottom 区域初始 row=lines-1 遮挡输入行（靠 reflow 校正，首帧闪烁）
</details>

<details>
<summary>文档（9）</summary>

- README L216 `/new` 的目录选择器浮窗未实现（openDirPicker 未用于 /new）
- README L315-316 180ms 实际 450ms；L344-345 「定时布局检查」实际是补全禁用
- REQUIREMENTS R-TEST-1 smoke.js→smoke.ts；§10 lib 平铺结构过时；R-NFR-4 files 白名单过时
- README L462 app-ops-check「19 个 ops」旧数字；L439 目录结构行重复；L413 漏 kernel/index.ts；L458 漏 ARCHITECTURE.md
- src/ext-api/index.ts:5-9 模块头注释「later phases」与已实现矛盾
- docs/EXT-API.md L12,L313 引用 src/app.ts（已迁 kernel/app.ts）
- REQUIREMENTS.md 被 .gitignore 排除但 README（随包发布）链接它 → 发布后链接失效
- REQUIREMENTS R-UI-K10 面板「52 列」实际 45% clamp 30-52
</details>

## 4. 🔵 信息项（55 项，摘要）

- **死代码**：kernel/index.ts 死 barrel；subagent-clean.encodeSessionLog 零引用导出；feed/whale.ts layoutWhaleRows/WHALE_ROWS 仅 smoke 引用；sessions/index.ts:259-266 四个 slice 包装零调用者；trajectory.ts step 死变量；subagents W 重复定义；cmd_menu/at_menu 若干冗余防御；images.ts `_knownMediaType?` 参数未用；`Buffer.from(base64)` 的 try/catch 不可达；nlcmd '?' 死条目、DESTRUCTIVE contains 死防护、被 patterns 先命中的死 exact 条目
- **注释漂移**：density/glance 注释错位；whale「min 40×22」；openUrl「fallback to echo」无实现；parsePluginYaml `--`/`__`；market/index 文档注释；subagent-clean 头注释；app.ts BUILD 注释错位
- **微泄漏/微性能**：ext-api listeners 空 Set 不清理、nodeFloats 不回收、sessionSubs 迭代中可变；table.ts Intl.Segmenter 每次构造；statusline jobs.list 查两遍；smoke.ts BISECT 调试日志残留
- **其他**：nvim.log 0 字节被 git 跟踪；sessions.ts 不可达防御分支/装饰性死行；export.ts 同秒覆盖/静默失败；archive 空白参数归档当前会话；queue.ts picker value 撞车；feed workflow 卡片串会话等

## 5. 修复执行计划（TOP15，按序执行）

- [ ] **Fix-1** session.lua 结构修复（R6）+ close_chat 完整实现（L6）+ `(vim.uv or vim.loop)`（R7）
- [ ] **Fix-2** ext-api：fireExtReady 调用 resolve（R3）+ luaExt.on disposer token 校验（R4）+ nvim.request/registerCommands 入参守卫（E1）
- [ ] **Fix-3** core.ts 审批/提问队列（R1、R2）
- [ ] **Fix-4** /rewind 数字索引语义 + 确认（R5）+ renderedDiffCalls 随 feed.clear 失效（S1）
- [ ] **Fix-5** feed：表格/diff 顺序（F1）+ ExtCard.update 叠加（F3）+ `??`→`||`（feed.ts:755）
- [ ] **Fix-6** tui_command 白名单收紧（C1）+ /quit /exit /restart 确认 + nlcmd 删 `['q']`（C2）
- [ ] **Fix-7** market 自卸载保护（F2）+ statusline jobs 去重（F4）
- [ ] **Fix-8** boot teardown 竞态（K1）+ 启动失败退出码/fatal 可见（K2）+ console 劫持治理
- [ ] **Fix-9** channelId 握手超时 + headless watchdog 前移（K3）+ openPicker settle 身份校验（K5）
- [ ] **Fix-10** yolo 路由方向（C3）+ /model 目录校验与真实选择器（C4）+ /image cwd（C5）
- [ ] **Fix-11** memory delete 确认（C6）+ workspace 假成功（S4）+ refreshHistory 吞错（S3）
- [ ] **Fix-12** ensureLiveSession in-flight 去重（S2）+ rename 后 dispose（S5）+ 后台 resume 不动全局视图（S6）
- [ ] **Fix-13** Lua：input 自愈挂载（L1）+ region q/Esc side（L2）+ at_menu 代数校验（L3）+ full_input footer 守卫（L4）+ region_claim pcall（L5）
- [ ] **Fix-14** smoke 假通过面（E3、恒真断言、build-first）+ e2e 口径（E5）+ session-stats 键名（E4）+ 执行层白名单契约（E2）
- [ ] **Fix-15** 文档同步：README 识图桥节（D1）+ LRU 声明（D2）+ §3 文档 9 项批量修正

## 6. 验证清单（每批修复后执行）

1. `npx tsc --noEmit` + `npx tsc -p tsconfig.scripts.json --noEmit`
2. `node scripts/check-arch.mjs` + `node scripts/app-ops-check.mjs`
3. `npm run build`（lib/ 与 src/ 保持逐字节同步，lib 是入库产物）
4. Lua 修改：luajit 语法检查（需要 vim stub）+ `node scripts/smoke.ts`（覆盖 luaCall 面）
5. 全部完成后：`node scripts/smoke.ts` + 如环境允许 `node scripts/e2e.ts`

## 7. 修复记录

| Fix | 状态 | 说明 |
|-----|------|------|
| Fix-1 | ✅ 完成 | session.lua 结构修复（SE.close_chat 恢复导出，toggle_reasoning 保留 `return S.reasoningOpen` 契约）+ 幽灵 buffer 走 ensure_reasoning + `(vim.uv or vim.loop)` |
| Fix-2 | ✅ 完成 | fireExtReady 经 readyWaiters 真正 resolve；luaExt.on disposer token 校验；nvim.request/registerCommands 入参守卫 |
| Fix-3 | ✅ 完成 | 审批队列删 setApproval(null,null)；questions 改走 settleQuestions/rejectQuestions（rejectQuestions 支持自定义 reason） |
| Fix-4 | ✅ 完成 | /rewind 数字从开头计数 + 数字路径确认；renderedDiffCalls 随 feed.clear 重置 |
| Fix-5 | ✅ 完成 | 表格检测移到 diff 区域关闭逻辑之后（smoke「table after diff fence」回归通过）；ExtCard.update 按上次合并态；`??`→`||` |
| Fix-6 | ✅ 完成 | 白名单移除 /settings /memory；quit/exit/restart 确认；nlcmd 删 ['q'] |
| Fix-7 | ✅ 完成 | market 自卸载保护改查未过滤集合；jobs 板去重加 committedJobKeys 持久排除 |
| Fix-8 | ✅ 完成 | boot 各 await 后查 disposed；启动失败退出码非 0（childExitDuringBoot 记录 + stderr fatal） |
| Fix-9 | ✅ 完成 | channelId 握手 3s 超时 + watchdog 前移布防（headless 守卫）；openPicker 失败结算身份校验 |
| Fix-10 | ✅ 完成 | yolo 固定参数路由 + 参数校验；/model 复用 modelCatalogRows + provider/model 校验；/image 按 activeSessionCwd |
| Fix-11 | ✅ 完成 | /memory delete 确认；workspace 可选服务显式检查；refreshHistory 失败 notice + exitDiag |
| Fix-12 | ✅ 完成 | ensureLiveSession in-flight 去重；rename 后台恢复会话完成/取消后 dispose（sessions slice op）；后台恢复不写全局视图状态 |
| Fix-13 | ✅ 完成 | input 自愈挂载；region q/Esc 传 side；at_menu 陈旧响应 '@' 守卫（smoke 测试 start 约定同步修正）；full_input footer 守卫；region_claim pcall+{err} |
| Fix-14 | ✅ 完成 | smoke graceful-exit 显式警告化（见 §8 已知问题）+ 恒真断言修正 + build-first；e2e 退出码校验；session-stats 键名；systemlist 移出白名单（契约三方对齐） |
| Fix-15 | ✅ 完成 | README 识图桥重写 / 450ms / 布局保护描述 / 目录结构；/new 目录选择器实现；REQUIREMENTS 入库+随包发布+陈旧项同步；CHANGELOG 未发布修复批 + LRU 声明更正；ext-api 头注释；EXT-API.md 路径 |

## 8. 遗留已知问题（本次未修，转后续）

1. **smoke graceful-exit 不达标**：本机环境 `:qa!` 后 3s 内 nvim 不自行退出（审查前即存在，非本批引入）。疑似 dsh_tui 的窗口守卫/浮动窗口在 quit 拆除过程中触发 WinClosed 重建，与 `:qa` 互相拉扯。smoke 现输出显式 ⚠ WARN 而非静默 PASS，待专项排查（可先试：teardown 时先摘除全部 autocmd 再 qa）。
2. §3/§4 其余 🟡/🔵（约 127 项次要问题）未在本批处理，见各自清单。
3. nvim 0.8 的 title 键兼容（README 最低 0.9，仅提示级）。
