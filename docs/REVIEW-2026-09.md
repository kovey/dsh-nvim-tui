# 全代码库审计报告（2026-09-10）

**方法**：14 个审查单元并行审计（模型：`deepseek-official/deepseek-v4-flash`，经 workflow 编排），
每条高/中危发现再由**第二个 flash 代理**做对抗性复核（读代码证伪/证实，共 30 条复核）。
审查覆盖 `src/**`（kernel / commands / sessions / feed / transcript / statusline / subagents /
market / deps / boot / ext-api）、`nvim/lua/dsh_tui/**`、`scripts/**` 与全部文档宣称。
共产出 **157 条发现**；各单元完整报告（含复现探针脚本要点与"已核查但判定不是问题"的清单）
在本目录下。

**上下文**：本次审查在 v0.1.5-rc.1 适配完成后进行，若发现项与"刚改动的代码"相关，
已在修复批次中一并处理。

## 本次已修复（随本文件同一提交）

| # | 问题 | 位置 | 修法 |
|---|---|---|---|
| 1 | **多行 base 行让整条渲染管线永久失效（E5108）**——workflow 阶段标题/stopReason/error/ext-card 按钮文案来自宿主与模型，未折叠换行 | `src/feed/feed.ts` flush 末行 | 末行统一 `replace(/\r?\n/g,' ')`（最后一道防线，覆盖全部入口） |
| 2 | **`toolActivity` 在 turn/end 不清除**：孤儿 tool/call 后底部永久挂假"运行中"行且 500ms 自续 flush 死循环 | `feed.ts` turn/end | 清 `toolActivity` + `subagents` |
| 3 | **subagent start/end 的 map key 兜底不一致**（`'?'` vs `''`）：无 runId 时条目永久泄漏 + 幽灵活动行 | `feed.ts` | 统一 `FeedRenderer.subagentKey()` |
| 4 | 交互卡片 extmark **不含尾行**：光标在动作提示行时 1-9/Enter 失效 | `feed.ts` | `end_row: endRow + 1`（与 api.lua `row < end_row` 对齐） |
| 5 | 流式围栏代码块**永远拿不到语法高亮 token** | `feed.ts` 增量高亮 | 过滤条件改区间相交 |
| 6 | `@` 补全的 `start` 参数在 App slot 包装层被丢弃 → 非行首 `@` 菜单错位 | `src/commands/index.ts` | 透传 `start` |
| 7 | 自然语言路由的**带参模式缺句子护栏**（"插件市场里有很多插件" 被静默改成命令） | `src/commands/nlcmd.ts` | 无分隔符且含句子助词 → 交给 agent |
| 8 | **provider→settings ns 靠猜**（0.1.5：`deepseek-official` ↔ `llm-deepseek`）→ `/models` 列不出默认 provider 的模型 | `commands/commands/models.ts`、`model.ts` | 经 `listConfigurableProviders().settingsNs` 解析；`ServiceMap` 补 `llm` 类型 |
| 9 | **子代理 running 语义错**（宿主 `activity` 只表示"驻留"，已结束的子代理被长期显示为运行中，并干扰 TTL 清理） | `sessions/index.ts` | 改用 TUI 子代理事件注册表 / `agent.status` |
| 10 | **rename 目标被替换/清空时后台 live 会话泄漏**（agent handle + nvim buffer 永不释放） | `commands/index.ts` | 替换/clear 时 `disposeLiveSession` |
| 11 | 恢复回放绕过 hook 表 → **会话标题丢失**（0.1.5 标题是 `session/title` 日志事件） | `sessions/services.ts` | 回放时恢复 `rec.title` |
| 12 | **`splitImageDataUrls` 只剥离第一条 data URL**（后续粘贴的图被当正文发给模型且不外发） | `feed/images.ts` | 专用全局 regex 扫描 |
| 13 | **`/restart` 后继失败被当成功**：依赖 python3，缺失时 `sh` 以 127 退出仍当重启成功（TUI 静默消失） | `kernel/lifecycle.ts` | 预检 python3（缺失回退直接 exec）+ 退出码/信号诊断写 stderr |
| 14 | **启动窗口 notice 被整体吞掉**（会话历史加载失败等真实故障提示全丢） | `kernel/app.ts` | `pendingNotices` 缓冲，首个活跃会话建立时 flush |
| 15 | 难度分类器把**超时/中断当合法的 medium**（不回退规则） | `kernel/difficulty.ts` | 校验 `finish.kind==='stop'` + 词表命中，否则回退规则 |
| 16 | 档位路由**目录校验挡不住错 id、effort 完全不校验** | `kernel/difficulty.ts` | 改用 `listModels` 成员判断 + effort 兼容校验/降级 |
| 17 | 识图切换携带模型不支持的 `reasoningEffort` → 回合在适配器前就失败（`UNSUPPORTED_REASONING_EFFORT`） | `commands/core.ts`、`kernel/vision.ts` | 新 `effortSupported()`，不支持则本回合去掉 effort |
| 18 | 死代码：无调用方的 `src/kernel/index.ts` barrel、重复定义的 `openLivePicker`、只写不读的 `${dumpPath}.applies`（且是 createApp 内唯一无保护的同步写）、todo-guard 不可达的 `events` 回退 | 多处 | 删除 |

## 各单元完整报告（本目录）

### boot-ext.md
- [bug / medium] 启动失败窗口：nvim 在 attach 前死亡 → 退出码 0，且可能完全静默
- [bug / medium] attach 之后、监听安装之前：nvim→runner 入站通道未接线（通知丢失 / 请求死锁）
- [bug / medium] attach 之前注册的 Lua 扩展：订阅永不送达 runner（会话事件镜像静默失效）
- [bug / medium] hmr 重载后 `tui:ready` 无人收到：一次性标记是模块级，补发缓存却是安装级
- [risk / medium] console 进程级劫持永不恢复：静默失败链与恒不可达的 e2e 断言
- [risk / medium] `api.ready` 无超时、不 reject，且 boot 存在多条「不再 announce」的出口
- [missing-feature / low] 「跨 hmr 存活」契约半实现：状态栏段与 `luaExt` 处理器每次 apply 都会丢
- [bug / low] `turn/end` 钩子无条件 `pendingFileSnaps.clear()`：跨会话踩掉在途 diff 快照
- [deadcode / low] `extReadyResolve` 只写不读
- [risk / low] `Promise.race` 的超时定时器在成功路径不清理

### commands-a.md
- · bug / high · 无活跃会话时「降级提示」全部被静默吞掉（本单元 7 处命中）
- · bug / medium · 目录选择器「取消」永不结算 → `/dir`、`/lines`、`/attach`（无参）永久挂起
- · bug / medium · 文档承诺的 `~/` 支持失效（C5 修复引入的回归；`readImageFile` 的 `~/` 分支已成死代码）
- · bug / medium · `/attach` 把「读取失败」当成「非图片」，且 luaCall 失败仍报成功
- · bug / medium · `/locale` 切换后命令目录仍是旧语言（`refreshCommandCatalog` 重发的是安装期冻结的字符串）
- · risk / medium · `openDirPicker` 失败兜底无条件清槽 —— 与已修复的 picker K5 同类竞态
- · risk / medium · `/compact` 无超时、无取消通道（`AbortController` 即用即弃）
- · risk / medium · `/attach` 对任意用户路径做无上限同步全量读（阻塞事件循环 / 内存放大）
- · bug / low · `/fb` 把「list 失败」当成「没有反馈记录」，并带 `ifVersion: null` 继续写
- · bug / low · `/config` 的「用户配置 已加载」与实际生效值不一致
- · risk / low · `bell.ts` / `image.ts` 绕过域操作直写 agent slice，`check-arch` 规则 3c 看不见
- · risk / low · UI 类 luaCall 失败被静默吞掉，命令表现为「什么都没发生」
- 服务装配 / 降级矩阵（本次重点核对项）

### commands-b.md
- [high / bug] `/model`、`/models` 的模型目录读取名字空间合成错误 —— 默认 provider 下整个“可切换模型”列表消失
- [medium / risk] `/permission` 的“危险预设”确认只按预设**名字**正则判定，自定义预设表可静默绕过
- [medium / bug] `/settings set` 把写入值原样回显，`role('secret')` 字段（API key）明文进聊天区
- [medium / bug] `/preset` 绕过 `agentPresets.select()`：丢失 per-session 串行化，且自实现的空白判定与宿主不同源
- [low / bug] `/theme default` 是空操作：预设只叠加不复位，旧主题属性永久残留
- [low / bug] `/steer` 的引导指令在实时聊天区渲染两次（➤ 行 + 用户气泡），回放时只剩一次
- [low / bug] `/memory` 列表只枚举顶层 `.md`，项目记忆的主体（`lessons/` 子树）不可见
- [low / deadcode] `configuredModels(app, providerId, settingsNs?)` 的第三参数无任何调用方
- [low / missing-feature] `/settings` 的自述用法与实现语法不一致（`set` 缺 ns），注册 usage 也只写 `[edit]`
- [low / risk] `/yolo` 手写 `approval/policy` 事件，绕过官方 `userApproval.setPolicy()`（无去重、无模型侧变更通知）
- [low / risk] `/panel` 是“无反馈 + 全吞异常”的裸 RPC，Lua 侧在非 TUI 窗口下是显式 no-op

### commands-core.md
- [bug / high] 自然语言路由的「带参模式」缺少问句/句子护栏，普通聊天气泡被静默改写成状态变更命令
- [bug / high] `@` 补全的 `start` 参数在 App slot 包装层被丢弃 → 非行首 `@` 的补全菜单永不弹出
- [bug / medium] 卡片的 input 交互在「以 `/` 开头」的输入上被绕过：文本被当成聊天发给模型，卡片却仍处于「等待输入」状态
- [bug / medium] 审批浮窗的「总是（自动模式）」对子代理审批静默降级为「允许一次」，且无任何提示
- [missing-feature / medium] `fileReferences.list` 调用失败时没有任何降级：`@` 菜单静默为空（文档只承诺「服务缺失」降级）
- [risk / medium] 三处「永不 abort 的 AbortController + 无超时」：`@` 查询与子代理续聊可能永久挂起且无任何反馈
- [bug / low] `SENTENCE_RE` 护栏覆盖不到它自己文档里举的例子：「帮我看看记忆」仍会被注入 `tui_command` 路由提示
- [bug / low] `/subagents → 继续对话` 的输入被乐观回声到**父会话** feed，父会话 transcript 出现从未收到的用户气泡
- [deadcode / low] 三个 owner op 无任何调用方（被同模块的直接写切片绕过）
- [risk / low] `send()` 先清空 `pendingImages` 再调用可能中途 return 的 `followup` → 附加失败时用户排队的图片被丢弃

### docs-claims.md
- · `/locale zh` 在“以 en 启动”后无法还原中文：命令描述在安装期被冻结，字典是单向的
- 附：已核实但未计入上表（同源、影响更低）

### feed-core.md
- base 中的多行字符串会让整条渲染管线永久失效（E5108）— bug / 高
- `toolActivity` 在 turn/end 不清除：孤儿 tool/call 之后底部永久挂着假"运行中"行 + 500ms 自续 flush 死循环 — bug / 高
- `subagentStart`/`subagentEnd` 的 map key 兜底不一致（`'?'` vs `''`）：无 runId 的子代理永久泄漏，产生幽灵活动行 + 500ms 死循环 — bug / 高
- 交互卡片 extmark 尾行不含：光标停在卡片最后一行（动作提示行）时 1-9 / Enter 失效 — bug / 中
- 流式围栏代码块永远拿不到语法高亮 token（增量窗口把块起始行排除在外）— bug / 中
- 步骤进度块被提升到"它前面的正文"之上：消息块序被倒置 — bug / 中
- deadcode：in-place 分支的 `codeBlocks.filter((b) => b.row === inPlaceRow)` 永远为空 — deadcode / 低-中
- 推理面板无自愈路径：缓冲被 wipe/重建后，面板静默永久停更 — risk / 中
- `syncCardMarks` 串行 + 单点 `.catch(()=>{})`：一次失败即中断整批卡片同步且无任何诊断 — risk / 中
- `commitJobsBoard` 不幂等，去重责任外置到调用方 — risk / 低-中
- i18n 缺口：子代理/注入上下文行与回合分隔行绕过 `t()` — missing-feature / 低

### feed-parts.md
- `splitImageDataUrls` 只剥离第一个 data URL，且把后面的正文吞进 URL / 从提示词里删掉 — bug / 高
- 鲸鱼动画帧引用了 6 个**未定义**的高亮组：像素用默认色渲染并在帧间闪烁 — bug / 中
- 空文件 diff：凭空多出一行 `+ ` 且 `+1 −0`；`''` 与 `null` 两条路径口径不一致 — bug / 中
- `estimateCost` 的定价表不含 dsh 默认模型 `deepseek-flash` 与识图模型 → 状态栏/`/cost` 静默不显示成本 — missing-feature / 中
- 声明类型与字节 sniff 不一致的 data URL 被静默丢弃：URL 文本已从提示词删掉，图没发出去，零提示 — bug / 中
- `maxWidth` 契约被调用方破坏：用户回显多加 `> ` 前缀、首次 flush 用过期的 100 列、列数过多时仍溢出 — risk / 中
- 剪贴板读图：`osascript`/`sips` 无超时（同步阻塞事件循环），`.png` 临时文件在异常路径泄漏 — risk / 中
- `wrapText` 每个单元格都构造一个 `Intl.Segmenter`，落在流式热路径上 — risk / 低-中
- 只认"两侧都有竖线"的 GFM 子集，且 `\|` 转义在单元格里不还原 — missing-feature / 低
- deadcode：`layoutWhaleRows` 无生产调用方（含 `WHALE_ROWS` 的导出）— deadcode / 低
- deadcode：`readImageFile` 的 `_knownMediaType` 参数从未被读取，两个调用方也都只传 1 个实参 — deadcode / 低
- deadcode：`parseImageDataUrl` 里 `Buffer.from(base64)` 的 `try/catch` 不可达 — deadcode / 低

### kernel-core.md
- [bug / high] `notice` 只有「当前活动会话 feed」一个 sink：启动窗口内的真实故障提示被整体吞掉
- [risk / medium] 进程级 `uncaughtException` / `unhandledRejection` 钩子只记日志，压制了 Node 的 fail-fast
- [risk / medium] `appExit` 服务在 createApp 期被一次性捕获：捕获失败即静默降级为 `process.exit()`
- [risk / medium] `liveSessions` 适配器把 `agents` 注册表在 createApp 期捕获：服务被重新 provide 后会话查询永久失真
- [deadcode / medium] `src/kernel/index.ts` 是无调用方的死 barrel（且不是对外可达的 API）
- [missing-feature / low] 0.1.5 新增的 `system/message` surface 事件没有类型成员也没有渲染分支，与升级文档的宣称不符
- [risk / low] 内核原语对「尚未注入的 slice 字段」用 `=== null` / `!== null` 判空：任何 install 前调用都会 TypeError
- [risk / low] `registerCommands` 对重名命令「静默拒绝」，扩展方拿不到失败信号
- [deadcode / low] `${dumpPath}.applies` 是只写不读的调试残留，且是 createApp 内唯一无 try/catch 的同步文件写
- [deadcode / low] `openLivePicker` 在 createApp 内被定义两次，第一份永不生效
- [risk / low] `SessionRec` 的尾随索引签名让 strict 失效：字段改名/拼错在编译期无声通过
- [risk / low] `ApprovalRequest.signal.removeEventListener` 声明了却从未被使用：abort 监听器只加不摘

### kernel-features.md
- 识图临时切换会被"任意在其后开始的回合"消费 → 图片回合跑在非识图模型上直接失败（bug，高）
- 分类器失败/超时被当成合法的 `medium`，未回退规则（bug，中）
- 识图选型不考虑 `reasoningEffort` 兼容性（bug，中）
- 档位路由的"目录校验"挡不住错别字，effort 完全不校验（bug，中）
- M4 子代理模型闸门：对当前会话不生效、破坏性写入全局设置、状态行谎报（missing-feature + risk，中）
- 识图切换挂起时，park 好的难度估计被丢弃（bug，低）
- 悬空切换的恢复路径漏了 `rec.provider`（bug，低）
- profile.ts 的 loader 回退分支必然抛异常且被静默吞掉（bug，低；同时是"doc 宣称的健壮性"缺口）
- `todo-guard` 的 `session.events` 回退分支在受支持宿主上不可达（deadcode，低）
- 待办守卫的注册与运行路径全量吞异常，功能可能静默消失（risk，低）

### kernel-runtime.md
- [bug / high] `/restart` 后继进程启动失败被当作成功：dsh 静默退出，没有后继
- [bug / medium] `closeNvimWindow` 的「返回时子进程必死」保证没有被强制，`quit()` 却无条件启动后继
- [bug / medium] 退出清理预算自相矛盾：硬退出 2000ms 早于 2.5s 清理窗口，且小于 `closeNvimWindow` 自身最坏耗时
- [bug / medium] 退出时的会话持久化 flush 链三重静默失败
- [bug / medium] i18n：字典键与调用点大面积漂移，英文模式静默退回中文（并留下大量死键）
- [bug / low] headless dump 的失败兜底自身会抛，且两个调用点都用 `void` 丢弃 → 宿主 fail-loud 直接杀进程
- [bug / low] dispose 撞上 `await spawnNvim`：子 nvim 被孤儿化并占住 tty
- [missing-feature / medium] `subagent-clean` 宣称的截断清理不存在：0.1.5 上「清理」只剩隐藏，存储永不回收
- [risk / low] 未知 nvim 通知逐条同步写盘：无去重、无限速，日志可被外部输入打爆
- [risk / low] `host-events` 的守护只覆盖同步 throw，注释宣称的「不会杀死宿主」对 async handler 不成立
- [risk / low] 终端模式重置不全，且 kitty 关闭用的是「弹栈」而不是无条件清零
- [deadcode / low] `encodeSessionLog` 的 events 编码分支没有调用方

### nvim-lua.md
- [bug / high] `show_skill` / `show_lines_float` 不关闭同族旧浮窗：孤儿窗口 + `q`/`Esc` 关错窗口（旧窗可能关不掉）
- [bug / high] `P.show_picker` 用 `close_float()`（缺 `notify_replaced`）：被顶掉的审批/提问浮窗永不结算，回合挂死
- [bug / medium] `at_menu.accept()` 的裁剪点钳到 `@` 起点而非 token 终点：光标落在 token 内/左侧时残留片段被拼回输入行
- [bug / medium] bottom 停靠区浮窗不随输入框增高重排 → 遮挡输入框，违反文档承诺
- [risk / medium] `on_lines = vim.schedule_wrap(...)` 里的 `return true` 无法 detach：注释宣称的"视图消失即解绑"从未发生
- [risk / medium] `S.buildingLayout` 无异常复位：一次 `botright 1split` 失败即永久关闭自愈/所有权守卫
- [deadcode / medium] 会话列表浮窗整族无生产调用方（仅 smoke 可用），`/sessions` 已改走通用 picker
- [bug / low] `DshTuiQuote`：`hi default ... gui=italic` 之后的 `hi default link ... Comment` 恒不生效 → 引用块失去暗淡色
- [deadcode / low] `layout.lua:58-66` 的 `has('nvim-0.9')` else 分支不可达，且该分支用的是 0.10 才有的 `virt_text_pos='inline'`
- [risk / low] `S.subagentView` 在**模块加载期**被无条件重置：重载后丢失活浮窗句柄，并复现作者刚修掉的 E95 静默失败
- [risk / low] `S.activeId == nil` 时按 `<C-o>` 每次泄漏一个未登记显示缓冲（`bufhidden='hide'`，永不回收）
- [deadcode / low] `API.panel_reflow` 全仓库零调用方

### sessions.md
- [bug / high] 后台 resume 的会话在「待重命名」被丢弃时永不释放（live 记录 + AgentHandle + nvim buffer 永久泄漏）
- [bug / high] `activity === 'running'` 被当作「回合进行中」：已结束的 continuable 子代理被永久标成运行中，TTL 清理永不触发，状态行幽灵计数在每次切换后被复活
- [bug / medium] 恢复会话后标题丢失（回放不跑 `session/title` 钩子），`/sessions` 活会话行又没有历史标题兜底
- [missing-feature / medium] `/sessions` 的「未分组」行是死行：README/REQUIREMENTS 宣称的分组从未实现
- [risk / medium] resume / create / fork 都没有失败回滚：live 记录与 AgentHandle 会带着「半挂载」状态滞留
- [risk / medium] `disposeLiveSession` 先删表项再 await dispose：该窗口内的 resume 会撞上宿主的 "session already exists"
- [bug / low] `/rename` 把可能为 `undefined` 的 live session 直接交给宿主 `sessionTitle.rename`，与同族路径的守卫不一致
- [risk / low] `dsh-session-new` 的宿主侧入口在产品内是孤儿路径，且实现与 `/new` 的目录口径不一致
- [deadcode / low] `AppSlices.sessions.attachSession` 槽无任何调用方
- [risk / low] `switchTo` 先改 `activeId` 再 await `lua.setActive`，失败无回滚
- [bug / low] 标题投影回退用了签名不兼容的服务，异常被本地 try/catch 吞掉（该回退永远拿不到标题）
- [risk / low] `listSubagentChildren` 静默丢弃宿主上报的 diagnostic 行（不可读子代理从目录里消失）
- 附：复核过但未单列的问题（低价值 / 已有记录 / 证据不足，供后续参考）

### subagents-market-deps.md
- [bug / high] `/deps install` 在本机实测为恒空操作：`packageExists` 的「dsh 安装根」永远解析不到宿主包所在目录
- [bug / high] `/market` 入口自修复链先 `remove` 再 `add`：候选源全失败时插件被留在「已卸载」，却报「已安装但入口缺失」
- [bug / medium] `installedMainMissing` 只认 `main`（默认 `index.js`）：`exports`-only 的包被判「缺入口」，进而触发破坏性换源
- [bug / medium] `installedMainMissing` 的 spec→包名模糊匹配：URL/tarball spec（或作用域名不在 URL 中）匹配不上，`pnpm remove <URL>` 静默失败 + 健康安装被误报
- [bug / medium] 子代理会话读取句柄在 `handle.read()` 抛错时泄漏（视图与对话两条路径重复同一段）
- [bug / medium] 运行 profile 的 `cordis.patch.yml` 不存在时，`findProfilePatchPath` 回退成目录扫描 → `/deps install` 可能写进**另一个 profile**
- [risk / medium] `readPatch` 任何读失败都返回 `''`，而 toggle 分支会用它**整文件重写** `cordis.patch.yml`（用户 patch 静默清空）
- [risk / medium] `runPluginCliP` 的 5 分钟「硬超时」只发 SIGTERM、无 SIGKILL/竞速兜底，且用 `'exit'` 而非 `'close'` 收尾 → 流程可永久挂起 / 尾日志截断
- [missing-feature / medium] 提示宣称「/subagents 可取消」但没有任何取消实现：pending 寻址会静默把下一条输入发给子代理
- [bug / low] `/deps` 的 pnpm 探测用 `stdio:'ignore'`，版本号恒为空（实测 `stdout === null`），且同步阻塞事件循环最长 5s
- [risk / low] `/deps install` 的幂等去重只看 patch 文件，而宿主 loader 对**跨层**重复 id 直接抛错
- [missing-feature / low] `src/market/index.ts` 模块头注释与实现漂移：宣称持有 progress float 驱动 / CLI runner / 修复链，实际只剩一行命令装配
- 附：复核过但未单列的问题（低价值 / 证据不足 / 与其它单元重复）

### transcript-statusline.md
- 非尾部自愈在 dsh 0.1.5-rc.1 上必然抛错并被 `catch {}` 吞掉——毒化会话依旧 400，用户零提示 — bug / 高
- `/rewind` 重建把同一 SessionRec 的用量再累加一遍，状态栏 Σ/成本/缓存/上下文全部虚高 — bug / 中
- `/rewind` 的核心能力在目标宿主不存在 → 命令恒为降级提示，但 README/REQUIREMENTS 仍宣称可用 — missing-feature / 中
- 切会话后 jobs 心跳不刷新：徽标/鲸鱼/钉底任务板最多滞后 30s — bug / 低-中
- `/queue` 的增删改不请求状态栏刷新 → `⏳ N` 徽标假数据（最长 30s） — bug / 低
- workflow 运行表跨会话共享且日志/阶段/代理数组无界增长 — risk / 低-中
- `/density` 只影响"此后新产生"的工具卡，切换后当前视图无任何变化且不持久化 — missing-feature / 低
- `/trajectory` 的 `step` 变量只写不读 — deadcode / 低
- 价格表缺 `deepseek-v4-flash-vision-exp`（及自定义/档位模型）→ 识图回合的 `$cost` 与 `/cost` 预估整段消失 — missing-feature / 低
- 附：本次核对后**判定无问题**的高危嫌疑点（避免后续重复排查）

## 第二批修复（高危项清理）

| # | 问题 | 修法 |
|---|---|---|
| 19 | `closeNvimWindow` 的「返回即子进程必死」保证未强制：SIGKILL 之后无终局判定，`/restart` 仍会 spawn 后继 → 双 nvim 抢 tty（历史乱码回归面） | 补终局 `alive()` 断言 + 改返回 `Promise<boolean>`；`quit()` 在 `close` 为 false 时**取消重启**并 stderr 提示 |
| 20 | 退出清理预算自相矛盾：硬退出 2000ms < `closeNvimWindow` 最坏 1950ms + 2.5s teardown 窗口（flush 被截断） | 预算改 5000ms（restart 9000ms）；硬退出前若子进程仍活先 SIGKILL + `exitDiag('hard-exit-orphan')` |
| 21 | 退出 flush 链三重静默失败（全失败也置 `flushed=true`、service 方法被脱 `this` 调用、无诊断） | 逐会话失败记 `exitDiag`；`flush.call(store)` 保 `this`；无可用 flush 时记 `session-flush-unavailable` |
| 22 | resume 回放抛错留下**半挂载 live 记录**（此后该会话永远打不开）+ create/fork attach 失败泄漏 AgentHandle | 新增 `rollbackAttach()`（删记录 + dispose + close_chat）；resume 回放 try/catch 回滚后 rethrow；create/fork 同步回收 |
| 23 | `disposeLiveSession` 先删记录再 dispose → 窗口内重开会撞宿主 `session already exists`（且双 FeedRenderer 写同一 buffer） | 改为**先 dispose 后 delete** + `disposing` in-flight 表；`ensureLiveSession` 先 await 在途 dispose |
| 24 | `/sessions` 的「未分组」是死行（宿主 workspace id 是 UUID，`ws:none` 永不匹配 → 静默 return），README 宣称的分组从未实现 | 真正实现：列未被任何 workspace 归属且未归档的会话（含其他目录），选中可打开 |
| 25 | `/rename` 把可能为 undefined 的 live session 交给宿主 | 补守卫 + 明确提示（与同族路径一致） |
| 26 | `@` 补全：服务**抛错**时菜单静默为空（只有"服务缺失"才降级）；三处 `new AbortController().signal` 永不 abort 无超时 | 抛错记 `exitDiag` 并降级本地扫描；`@` 查询 4s / 子代理 prompt 30s 有界信号 |
| 27 | 推理面板无自愈：reasoning buffer 被 wipe/重建后面板静默永久停更 | 失败即重取 `reasoningBuf` + 强制重绘；连续失败 3 次推一次性告警行 |
| 28 | `syncCardMarks` 串行无隔离：一张卡片失败即中断整批（后续卡片不可交互 + 陈旧 mark 不清理 + 无诊断） | 逐卡片 try/catch + 首次失败诊断；陈旧 mark 清理同样隔离 |
| 29 | **入站通道接线晚于 attach**：`User DshTuiAttach` 里扩展的 `rpcrequest` 无应答 → Lua 侧阻塞在 attach 内**双向死锁**，rpcnotify 全丢 | `disconnect/request/notification` 监听提前到 connect 之后、attach 之前 |
| 30 | 启动失败窗口退出码 0：`nvim` 赋值后 attach/wiring 阶段死亡走 `quit(0)`（置 `quitting` 使 catch 的 quit(1) 变 no-op），CI/包装脚本把失败判成功 | 引入 `wired` 阶段标志；未 wired 的死亡一律记 `childExitDuringBoot`；catch 在 `quitting` 已置位时直接 `process.exit(1)` |
| 31 | `console.log/warn/error` 进程级劫持永不恢复（runner row 重载后宿主日志全丢） | teardown 时经 hostDisposer 恢复原函数 |
| 32 | `api.ready` 无超时且有多条"不再 announce"出口 → 消费方初始化永久静默挂起 | 30s 有界：超时 resolve（不 reject，避免 fail-loud 硬杀）+ `exitDiag('ext-ready-timeout')` |
| 33 | credentials 探测无超时，卡住即冻结 boot 序列（界面已可见但 `announceReady` 永不发生） | 2s 超时按"未配置"处理 |


## 第三批修复（中危项清理）

| # | 问题 | 修法 |
|---|---|---|
| 34 | 无活跃会话时「降级提示」全被静默吞掉（20+ 处命令表现为"输入命令毫无反应"） | 已由第二批的 `pendingNotices` 兜底覆盖：无 feed 时入队、首个活跃会话建立时 flush |
| 35 | 目录选择器「取消」永不结算（`q`/`Esc`/到根/读目录失败只关浮窗，不发通知）→ `/dir` `/lines` `/attach` 无参版永久挂起 | `close_dir_picker(keep_silent)`：取消路径补 `dsh-dir-selected`(nil) 结算；选中/取代路径传 `true` 保持静默（否则会先以 nil 结算、丢掉选中的路径） |
| 36 | `~/` 展开在调用方之前失效（`isAbsolute('~/a.png')===false` → 拼成 `<cwd>/~/a.png`）→ `/image`、`/attach` 文档承诺的 `~/` 支持是死代码 | 新增 `expandHome()`，两个调用方在相对/绝对判定**之前**展开 |
| 37 | `/attach` 把「无活跃会话」误报为「附件服务未装配」 | 拆分两种原因，分别提示 |
| 38 | 空文件 diff 凭空多一行 `+ ` 且计 +1（`''.split('\n') === ['']`） | `addOnly`/`delOnly` 对空串返回 0 行 0 统计 |
| 39 | `estimateCost` 定价表缺 `deepseek-flash`（**0.1.5 出厂默认模型**）与识图模型 → 状态栏 `$` 段与 `/cost` 静默消失 | 补齐 4 个 catalog 模型（含 vision-exp） |
| 40 | 剪贴板读图 `osascript`/`sips` 同步无超时（卡死即冻结整个事件循环）；`.png` 临时文件在异常路径泄漏 | `timeout: 5000 + SIGKILL`；PNG 清理移入 `finally` |
| 41 | 鲸鱼动画引用 **6 个未定义高亮组**（`-E -W BE BP EB PW`）→ 像素回落默认色并在帧间闪烁 | highlight.lua 补齐 6 组（同调色板） |
| 42 | `wrapText` 每个表格单元格新建 `Intl.Segmenter`（流式热路径） | 提取模块级共享实例 |
| 43 | `/settings set` 把机密值（api key/token/secret）回显进聊天记录 | 键名或值命中机密模式时显示 `••••（已隐藏）` |
| 44 | `/compact` 无超时不可中止（provider 卡住即永久等待） | 180s 有界 `AbortSignal.timeout` |
| 45 | `/permission` 危险判定用**名字正则**（重命名/别名预设可绕过确认） | 改用 `permission.resolve(name)` 的沙箱/审批旋钮判定（`danger-full-access` 或 `approval: never`），名字正则仅作兜底；确认框与结果提示带上旋钮 |
| 46 | 步骤进度块被提升到其**前面的正文之上**（任何未完成步骤期间消息块序倒置） | 拼装顺序改回 `restTail → progressLines` |
| 47 | `turn/end` 无条件 `pendingFileSnaps.clear()`：跨会话踩掉在途 pre-edit 快照 → ✎ diff 静默丢失 | 快照记录 owner 会话，turn/end 只清本会话（无 owner 的遗留仍清） |
| 48 | `commitJobsBoard` 不幂等（心跳与终态竞争会重复落板） | 增 `batchKey` 参数（调用方传 id:status 批身份），同批重复提交为 no-op；**不按内容去重**（不同批次可能渲染相同） |
| 49 | `switchTo` 先改 `activeId` 再 await `setActive`，失败无回滚（runner 与可见视图失配） | 失败回滚 `activeId` + 诊断 + 提示 |
| 50 | `Promise.race` 超时定时器在成功路径不清理（每次有界调用滞留最长 30s 定时器） | 新 `withTimeout()` 助手，成功/失败均 `clearTimeout` |
| 51 | 死代码：in-place 分支的 `codeBlocks.filter(b => b.row === inPlaceRow)` 恒空 | 删除该分支及恒假的 token 块 |
| 52 | 「跨 hmr 存活」契约半实现（状态栏段 / `luaExt.on` 处理器重载即丢）却宣称完整 | 代码注释 + `docs/EXT-API.md` 新增「热重载与重新注册」小节（明确哪些需要重新注册与推荐写法） |
| 53 | i18n 漂移不可见（142 死键 / 155 未翻译，en 模式静默回落中文） | 新增 `npm run i18n:report`（`scripts/i18n-check.mjs`）精确量化三类漂移；本次实测 298 键 / 311 引用 / 142 死键 / 155 未翻译 / 4 动态点。批量补译留作待办 |


## 第四批修复（依赖一致性）

| # | 问题 | 修法 |
|---|---|---|
| 54 | 依赖树 / lockfile 停留在 **0.1.2-rc.1**，与 peer 声明的 `^0.1.5-rc.1` 及"已适配 0.1.5-rc.1"的结论不符（`npm install` 因 peer 冲突 ERESOLVE 失败） | devDep 提到 `^0.1.5-rc.1`，清理并**重新解析**依赖树：`node_modules` + lockfile 现为 0.1.5-rc.1（dsh-agent/dsh-llm/dsh-tools）。**在 0.1.5 类型面/实现面上重跑全量门禁**：`tsc` + arch/app-ops 门禁 + `SMOKE PASS`，并复跑 0.1.5 / 0.1.2 双宿主真机无头回归（均 exit 0）——这次同时验证了"编译期对 0.1.5 而非 0.1.2" |


## 第五批修复（剩余中危 / 死代码 / 文档口径）

| # | 问题 | 修法 |
|---|---|---|
| 55 | **`/deps install` 恒为空操作**：`packageExists` 只按 profile 根解析，而 11 个宿主插件全在 **dsh 安装根**的 `node_modules` 下 → 所有装配行被「跳过」（实测本机 11/11 均为 `profile=no`） | 新增 `findDshPackageDir()`（从 `process.argv[1]`/自身位置向上找 `@deepseek-ai/dsh` 的 package.json），root 候选含 dsh 包目录、`lib/node_modules`、profile 根与环境变量覆写；实测真实宿主 11/11 命中、不存在的包为 false |
| 56 | **`/market` 自修复链先 remove 再 add**：候选源全失败时插件停在「已卸载」，却报「已安装但入口缺失」 | 候选全失败后**恢复原安装**并如实提示（恢复失败则给出可照抄的 `dsh plugin add <spec>`） |
| 57 | `installedMainMissing` 只认 `main`：`exports`-only 包被误判缺入口 → 触发破坏性换源 | 支持 `exports`（递归取 string target，命中任一存在文件即通过） |
| 58 | spec→包名用 `includes()` 模糊匹配：URL/tarball spec 匹配不上（`pnpm remove <URL>` 静默失败）且会误匹配无关包 | 新增 `packageNameCandidates()`（scoped/**@version**/owner-repo/.git/tarball/`file:`），**无法解析时不再谎报缺入口**（返回 null，避免破坏性换源） |
| 59 | 子代理冷读句柄在 `handle.read()` 抛错时泄漏（视图 + 对话两条路径同形） | 两条路径均改 `try/finally` 关闭句柄 |
| 60 | `findProfilePatchPath` 在运行 profile 无 patch 时回退目录扫描 → `/deps install` 可能写进**另一个 profile** | 运行 profile 可解析时**直接返回其路径**（缺文件由写入路径创建）；扫描仅在完全无法解析时作为最后手段 |
| 61 | `readPatch` 任何读失败都返回 `''`，而 toggle 会用它**整文件重写** → 用户 patch 被静默清空 | 仅 `ENOENT` 视为空；其他错误抛出，market 命令捕获后**中止且不写入** |
| 62 | `runPluginCliP` 5 分钟"硬超时"只发 SIGTERM、用 `'exit'` 收尾（pnpm 持锁时忽略 SIGTERM → 可永久挂起；尾日志截断） | SIGTERM 后 5s 无条件 SIGKILL；改 `'close'` 收尾（stdio 已 flush），并打印终止信号 |
| 63 | `/rewind` 重建把同一 SessionRec 的用量**再累加一遍** → Σ/成本/缓存/上下文全部虚高 | 重建前重置 folded 累加器（usage/lastUsage/cacheReported/contextWindow/todos）再回放 |
| 64 | 切换会话后 jobs 心跳不刷新（徽标/鲸鱼/任务板最多滞后 30s） | `switchTo` 内立即 `refreshBgJobs + ensureSpinner + updateStatusline` |
| 65 | `/queue` 增删改不刷新状态栏 → `⏳ N` 徽标假数据 | 清空/移除后补 `updateStatusline()` |
| 66 | `/locale zh` 在 en 启动后无法还原中文（命令描述安装期冻结 + 字典单向） | `t()` 增加反向索引（en→zh），**catalog 改为推送时翻译**（`commandCatalog`/`refreshCommandCatalog`），`/locale` 重新推送即完成双向切换（实测 zh→en→zh 往返一致） |
| 67 | 死代码：`AppSlices.sessions.attachSession` 槽（只赋值无调用）、`readImageFile` 未用参数、`/trajectory` 只写不读的 `step`、`layoutWhaleRows` 未标注 test-only | 删除前三项（域操作 78→77，门禁同步通过），后者补注释说明由 smoke 覆盖 |
| 68 | 文档宣称与实现不符：`/rewind`（0.1.5 无 `session.truncate`）、`/search`（默认 `openAt: never` 未启用）、`dsh-vision-bridge`（v0.3.2 已移除仍被推荐）、R-SESS-3（实为跨目录展示）、R-PLUG-1（无 300ms/1.2s 自动重建）、状态栏 180ms（实为 450ms）、退出 2.5s（实为 teardown 2.5s + 硬兜底 5s/9s） | README 与 REQUIREMENTS 逐条改为与实现一致（含规避建议） |


## 第六批修复（i18n 补译）

| # | 问题 | 修法 |
|---|---|---|
| 69 | en 模式**静默回落中文**：155 条被引用的字符串无英文条目；另有 142 条字典死键（旧文案残留） | 补齐 **154 条英文翻译**（用法/提示/服务未装配/确认框/命令描述等），删除 **142 条死键**。补译时顺带发现并修复两个工具缺陷：①字典里存在**未加引号的 CJK 标识符键**（`新增: 'Added'`——CJK 是合法标识符字符），检查脚本与清理脚本的解析都漏了它们，导致「死键」统计偏差并一度引入重复键（TS1117）；②检查脚本只扫 `src/`，漏掉 `scripts/`+`examples/`（smoke 有 600+ 处 `t()` 断言） |
| 70 | 漂移指标不可信（空字面量与注释里的示例被计入「未翻译」；`const EN_DICT` 声明行被当成死键） | 检查脚本重写键解析（带引号 / 裸标识符 / 拒绝关键字与含空白形式）、跳过空字面量与注释行、加 fallback 探针白名单。**当前实测：310 键 / 311 引用 / 0 死键 / 0 未翻译 / 6 动态点** |

| 71 | **`t()` 用在模板字符串上永远命中不了字典**（字典保存的是未插值原文，调用点传的是已插值串）→ en 模式这 7 处恒显中文 | 新增 `tf(zh, vars)`（`{name}` 占位符），转换 session-events/feed 的 subagent·workflow 行等 7 处；顺带包装并翻译 statusline（`🔧 后台 {n}`/`缓存 {n}%`/`上下文 {n}%`/后台任务完成）、子代理视图与对话窗提示、会话「（无标题）」占位等 20+ 处高可见 UI 串 |
| 72 | 漂移工具只认 `t()`：`tf()` 的键被误报为死键（15 条），且没有「完全绕过字典」这一类的指标 | 调用点解析改为两遍（首参为字面量的调用 + 非字面量→动态点），`tf()` 纳入键解析；新增第三指标 **未走 `t()`/`tf()` 的中文字面量（当前 489 行）**。最终指标：**329 键 / 330 引用 / 0 死键 / 0 未翻译 / 489 未包装** |


## 第七批修复（i18n 第二层：未包装字面量）

| # | 问题 | 修法 |
|---|---|---|
| 73 | **489 行中文字面量完全绕过字典**（通知/状态栏/选择器/进度日志在用模板串拼字符串，从未经过 `t()`），en 模式一律显示中文 | 写了一个**保序、逐文件构建校验**的包装器：纯字面量 → `t('…')`；模板串 → `tf('…{0}…', [expr])`（**每个插值独立占位符**，求值顺序与次数与原代码逐字一致）。共包装 **235 行 / 56 个文件**；模型侧提示（`todo-guard` 段落）、`nlcmd` 意图匹配表、RPC 诊断标签、正则与配置键**明确跳过** |
| 74 | 包装后新增 184 条待译文案 | 由 `deepseek-v4-flash`（workflow 编排）**直接读取清单文件**分片翻译——避免人工转录错误；合并前用脚本逐条校验「无伪造、无遗漏」，两批均 100% 通过 |
| 75 | 首轮翻译曾出现**伪造键**（我手写批次时的转录错误，导致 140 条字典条目在代码中并不存在） | 用「从源码重新解析 + 与实际调用点比对」的方式发现（死键 140 条），**已全部删除**并改为文件驱动流程；最终指标 **563 键 / 564 引用 / 0 死键 / 0 未翻译**；未包装字面量 **489 → 244**（余量：`nlcmd` 匹配表 66、模型侧提示、RPC 诊断标签、正则/配置键等**本就不应翻译**的项） |


## 第八批修复（i18n 技术性文案 + 低危项清理）

| # | 问题 | 修法 |
|---|---|---|
| 76 | 余下的 RPC 诊断标签 / `label:`·`message:` 字段 / UI 变量赋值 / 模板插值内字面量仍是中文 | 第四轮包装（73 行，含 `registerNvimNotification` 标签、`classifyPnpmError` 消息、picker 动作标签、主题/工作区提示），补译 65 条；**字典最终 628 键 / 631 引用 / 0 死键 / 0 未翻译**；未包装字面量 489 → **169**（余量= `nlcmd` 意图匹配表 66、模型侧提示、RPC 名称、正则/配置键等**不应翻译**项） |
| 77 | workflow 运行表**跨会话共享**且 `phases/agents/logs` 无界增长 | `WorkflowRun.sessionId` + `/workflow` 只显示当前会话的运行；三数组各设上限（200/200/400，超出丢最旧） |
| 78 | `/density` 只影响**此后新产生**的工具卡（当前视图不变）且不持久化 | 切换后立即 `feed.flush()` 重绘；偏好写入状态文件（`saveUiPref`/`uiPref` 域操作 + `sessions/prefs.ts`），attach 时恢复 |
| 79 | `/theme` 切回 `default` 不会清除上一预设的属性（预设只增不减） | 预设表抽到 `kernel/theme-presets.ts` 共享；应用时**下发全部受影响组**（未设置的组给空 spec），Lua 侧把空 spec 视为「重置为默认」；偏好持久化并在 boot 恢复 |
| 80 | `estimateCost` 对自定义/档位模型 id 静默无价（`$` 段消失） | 精确表未命中时按族名回退（`*flash*` / `*pro*`），仍标注为估算 |
| 81 | `readImageFile` 用 `readFileSync` **整读**文件再嗅探（大文件阻塞事件循环）；`parseImageDataUrl` 的 `try/catch` 不可达 | 改为 fd 读 **512B 前缀**嗅探 + 扩展名回退，确认后只读一次；删除不可达 try/catch（`Buffer.from` 不抛） |
| 82 | `/fb` 把「列表失败」与「没有反馈」混为一谈（用 `ifVersion: null` 静默丢更新） | `list` 结果类型改为可判别联合；失败时明确报错并中止 |
| 83 | `/memory` 只列顶层 `.md`（项目记忆是嵌套目录） | `readdirSync(..., { recursive: true })` |
| 84 | `/subagents` 提示"可取消"但**没有取消实现**（下一条输入被静默发给子代理） | 实现取消：有待发子代理时再次 `/subagents` 即取消并提示 |
| 85 | `/deps install` 幂等去重只看 patch 文件（跨层重复 id 会让宿主 loader 组合失败） | 去重集合并入**运行中 loader 的现有行 id** |
| 86 | `/deps` 的 pnpm 探测 `stdio:'ignore'` → **版本号恒为空**，且同步阻塞最长 5s | 捕获 stdout（实测 `pnpm 10.34.5`），超时降到 2s |
| 87 | `profile.ts` 的 loader 回退分支对生成器调 `.find()`（必然抛错并被吞） | 先展开再查找；`todo-guard` 注册/运行失败改为 `exitDiag` 可见（此前整条功能可能静默消失） |
| 88 | `src/market/index.ts` 模块头注释与实现漂移（宣称含进度浮窗/CLI runner/修复链） | 注释改回真实职责（只装配 `/market` 命令，其余在 `commands/market.ts` / `progress.ts`） |


## 第九批修复（TS 严格性：把"保留项"按标准做法根除）

| # | 问题 | 修法 |
|---|---|---|
| 89 | **`SessionRec` 的尾随索引签名** `[key: string]: unknown`：字段改名/拼错在编译期无声通过（审计列为"需大改"保留项） | 直接移除并修完所有暴露出的访问（**0 处报错**——说明字段本就齐备，索引签名只掩盖错误）。同时给**有意保留**的宿主载荷索引签名（attachment ref / content block）与 `RunnerConfig`（用户 YAML）补上「为什么允许」的注释 + 明确"我们写出的字段都必须声明" |
| 90 | **`ApprovalRequest.signal.removeEventListener` 只声明不使用**：`{ once: true }` 只在 abort **触发**时移除监听器，正常应答的请求会把闭包（含 entry/app）挂在回合 signal 上直到其生命周期结束 | 类型改用宿主同款标准 `AbortSignal`（`readonly signal?: AbortSignal`）；审批与问答两条路径都保存 handler 引用并在**结算时 `removeEventListener`**（abort 与正常应答都摘除） |
| 91 | 其余标准严格开关未开（`noUncheckedIndexedAccess` / `noPropertyAccessFromIndexSignature` / `exactOptionalPropertyTypes` / `verbatimModuleSyntax` / `noImplicitOverride` / `noFallthroughCasesInSwitch` / `noImplicitReturns` / `allowUnreachableCode:false`） | **全部启用**并修完全部暴露点：索引访问 **71** 处（逐处加显式 guard，不用 `!` 断言）、动态属性访问 **63** 处（`process.env.X` → `process.env['X']` 等显式写法）、可选属性显式 undefined **36** 处（内部类型标 `?: T \| undefined` 自述语义；**宿主边界**如 ModelSelection 改为条件展开，保证"缺省≠显式 undefined"）。`npm run check`（src + scripts 两套 tsc）+ arch/app-ops 门禁 + SMOKE 全绿 |


---

## 待办（未修，按优先级）

**说明**：i18n 第三指标余量 169 行中，绝大部分是**本就不该翻译**的项（`nlcmd` 意图匹配表 66 行、模型侧提示、RPC 诊断标签、正则/配置键），仅少量技术性日志保留中文。

**低（余量，均为风格/特性类，非隐患）**：`commands-a/b` 中若干被直接写切片绕过的 owner op（域操作清理）、`nvim-lua` 单元报告的少量风格项、`feed-parts` 的 GFM 表格仅认双侧竖线子集（特性增强）

**高**
- `boot-ext`：`Promise.race` 超时定时器成功路径不清理、`extReadyResolve` 只写不读等剩余项（见单元报告）
- `transcript-statusline`：queue 编辑竞态、状态栏 jobs 心跳窗口（见单元报告）
**中**
- i18n：**批量补译**（漂移已可用 `npm run i18n:report` 量化：142 死键 / 155 未翻译）
- `preset.ts` 绕过官方 `agentPresets.select`；`yolo.ts` 绕过官方审批 setPolicy

**低 / 死代码**
- `transcript`：`sessions.attachSession` slot、`dsh-session-new` 孤儿浮窗无生产调用方
- `feed-parts`：`layoutWhaleRows`/`WHALE_ROWS` 无调用方；`readImageFile` 的 `_knownMediaType` 参数未用；`parseImageDataUrl` 不可达 try/catch
- `commands-a/b`：owner op 无调用方、`lines.ts`/`help.ts`/`difficulty.ts` 的 luaCall 失败被静默吞

## 文档宣称 vs 实现（docs-claims 单元）

- `/rewind` 在受支持宿主上恒不可用（`session.truncate` 已移除），README/REQUIREMENTS 仍写作已交付
- `/search` 开箱即空（bundle patch 未启用索引），文档按"已可用"宣传
- REQUIREMENTS 仍把已删除的 `dsh-vision-bridge` OCR 桥写成推荐识图路径
- `/locale zh` 在 en 启动后无法还原中文（命令描述安装期冻结 + 单向字典）
- R-SESS-3"只显示当前 cwd 会话"与实现相反；R-PLUG-1 的 300ms/1.2s 重建、状态栏 180ms 刷新（实际 450ms）
